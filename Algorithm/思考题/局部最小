在一个数组长度大于2，数字不想等的数组中，寻找到一个局部最小的值
步骤
1.先判断首尾两个数数否为局部最小，仅仅需要判断与他相邻的那个数是否比他大
2.如果不满足，则说明这两头的数都比他相邻的数字大，则在数组中间必存在一个局部最小的数
3.选择middle为判断点，查看m-1 和 m+1，
4.如果middle不满足局部最小，则比较m-1 和 m+1的大小，
5.如若m-1小，则在m和0之间继续二分，
6.否则在m+1和n-1之间二分。
