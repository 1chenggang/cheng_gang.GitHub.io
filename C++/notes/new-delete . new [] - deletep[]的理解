new-delete . new [] - deletep[]的理解
delete操作对于编译器来说执行的是两步，一步清理new分配的空间，一步清理这段空间的指针。
delete[]操作 意味着会告诉编译器，这里不止一个空间，当我们使用new[]分配内存的时候，实际上是分配了
            多组空间进入内存，然后将这这个总空间包装后，形成编译环境，
            于是在我们删除的时候，要对这里面的多个空间进行删除。
            
            
  code：

 String*  p =  new String[3];
  delete [] p;
  
  否则 将会删除头指针，但是留下两个空的内存。
